This folder contains the Rust code developed by Team PARSE for CSAW ESC.
To execute it, change the directory to this folder, install rust cargo if not available, and use the command cargo run in the terminal.
The code is present in Rust_code/src/main.rs